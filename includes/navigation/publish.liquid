<nav class="navigation" role="navigation">
    <a class="navigation__link" href="/">
        <span class="navigation__link-icon">🏠</span>Home
    </a>
    {% if appState == 'Guest' %}
        <a class="navigation__link" href="/login/"><span class="navigation__link-icon">🔑</span>Login</a>
    {% endif %}
    {% if appState == 'User' %}
        {% for navItem in postsNavigation %}
        <a class="navigation__link" href="{{navItem.endpoint}}">
            <span class="navigation__link-icon">{{navItem.icon}}</span>{{navItem.name}}
        </a>
        {% endfor %}
    {% endif %}
</nav>
